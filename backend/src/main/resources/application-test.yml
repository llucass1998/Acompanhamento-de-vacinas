spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///vacina_kids_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    clean-disabled: false

app:
  jwt:
    secret: chave_secreta_para_testes_que_precisa_ser_bem_grande_para_o_hmac_sha256
    access-expiration-minutes: 15
    refresh-expiration-days: 7
  cors:
    allowed-origins: "*"
