# Contratos da API - Vacina Kids

A API estará exposta sob `/api/v1` com suporte nativo a CORS configurado. 
As rotas de segurança exigirão `Authorization: Bearer <token>`.
Detalhes e Schemas serão expostos dinamicamente pelo Swagger UI em `/swagger-ui/index.html`.
